<keymap>
    <Home>
        <keyboard>
            <backspace>RunScript(script.homebackexit,exit)</backspace>
            <escape>RunScript(script.homebackexit,exit)</escape>
        </keyboard>

        <remote>
            <back>RunScript(script.homebackexit,exit)</back>
        </remote>
    </Home>
</keymap>